#
# deploy [argumets]
# This interface manages the deployment of the service stacks (all types) and the repository (module type only).
#
# deploy [up/downup/down]
# This method deploys, redeploys and removes the service stack(s).
#
# deploy module
# This methods deploys all the prerequisites required to be in place before the module can be run.
#
